#include <iostream>
#include "stack_calc.h" // Подключаем ваш класс PostfixCalculator

int main() {
    // std::string postfixExpression = "3 4 + 2 *"; // Пример постфиксного выражения

    // PostfixCalculator<int> calculator; // Создаем объект калькулятора для типа int

    // try {
    //     int result = calculator.evaluatePostfix(postfixExpression); // Вычисляем результат выражения
    //     std::cout << "Result: " << result << std::endl;
    // } catch (const std::exception& e) {
    //     std::cerr << "Error: " << e.what() << std::endl;
    // }

    // return 0;

    assert_calc();
}
